version: 1.0.{build}
branches:
  only:
  - master
before_build:
- cmd: nuget restore PetsWonderland/PetsWonderlandSystem.sln
build:
  project: PetsWonderland/PetsWonderland.sln
  publish_nuget: true
  include_nuget_references: true
  verbosity: minimal
